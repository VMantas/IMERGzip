streamlit==1.25.0          # Ensure compatibility with Streamlit Cloud
geopandas==0.14.0          # For working with geospatial data and geometries
rasterio==1.3.8            # For reading GeoTIFF files
shapely==2.0.1             # For working with geometric objects (Point)
requests==2.31.0           # For handling HTTP requests (fetch GeoTIFF)
geopy==2.3.0               # For geocoding ZIP codes to coordinates
numpy==1.23.5              # For handling numerical operations
